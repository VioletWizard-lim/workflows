name: Keep Streamlit Awake

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: curl -I -L --max-time 30 "talk-trace-ai-osdisgdz5t4yszzxecybbv.streamlit.app" || true
